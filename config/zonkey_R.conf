# --- I2Cドライバーの有効化 (これが不足していました) ---
CONFIG_I2C=y

# --- マウス・ポインティング機能 (Cirque Pinnacle用) ---
CONFIG_ZMK_MOUSE=y
CONFIG_ZMK_POINTING=y

# --- ディスプレイ機能 (OLED用) ---
CONFIG_ZMK_DISPLAY=y

# --- 通信の安定化 ---
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
