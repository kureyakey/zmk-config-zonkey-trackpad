# マウス・入力機能の基本
CONFIG_ZMK_MOUSE=y
CONFIG_INPUT=y
CONFIG_I2C=y

# Kureyakey氏のドライバを有効化するためのスイッチ
# 依存関係を確実にするため、I2Cを明示的に指定します
CONFIG_PINNACLE=y

# 万が一名前が違う場合のための予備
# CONFIG_INPUT_PINNACLE=y
# CONFIG_CIRQUE_PINNACLE=y

# 警告でビルドを止めない（これが最後の手段です）
CONFIG_ZMK_KCONFIG_WARNING_ERROR=n
